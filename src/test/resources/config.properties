# Application URLs
base.url=https://quloi.myworkrig.com/

# Credentials
username=anshul.gaur
password=vd8m5791

# Browser Configuration
browser.type=chrome
headless.mode=false

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=30
page.load.timeout=30

# Test Configuration
retry.failed.tests=true
max.retry.count=2
parallel.execution=true
thread.count=3

# Reporting
extent.report=true
extent.report.title=Workrig Automation Report
extent.report.path=test-output/reports

# Screenshot Configuration
take.screenshot.on.failure=true
screenshot.path=test-output/screenshots

# Logging
log.level=INFO
log.file.path=test-output/logs 